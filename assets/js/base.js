// Search in page
function searchInput() {
    var search = document.getElementById('seaechbox').value;
    window.alert('Có phải bạn muốn tìm là ' + search);
}

// Send Feedback
function sendFeedback() {
    var text = document.getElementById('contentFB').value;
    window.alert("Cảm ơn về phản hồi của bạn!");
}